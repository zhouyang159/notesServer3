spring:
  application:
    name: NoteApplication
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: http://113.44.7.213:8848/ # huaweiyun nacos
  main:
    web-application-type: servlet
  jackson:
    #日期格式化
    #date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dk
    password: consoleA1
    url: jdbc:mysql://rm-wz99kfxt13jnuimburo.mysql.rds.aliyuncs.com:3306/notes?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true

server:
  port: 8081
  servlet:
    context-path: /api/note

logging:
  file:
    name: ./logs/Note/NoteService-%d{yyyy-MM-dd}.log
    path: ./logs


